
/**
 * This is a re-export file to maintain compatibility with shadcn/ui structure.
 * The actual implementation is in @/hooks/use-toast.ts
 */
import { useToast, toast } from "@/hooks/use-toast";

export { useToast, toast };
